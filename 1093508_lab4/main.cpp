#include <iostream>
#include "queue.h"
#include "queue.cpp"
using namespace std;
int main()
{
    int size = 0;
    cin >> size;
    Queue idealQueue = Queue(size);
    Queue waitingQueue = Queue(size);

}